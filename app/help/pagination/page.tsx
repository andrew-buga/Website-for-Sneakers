import InfoPageLayout from "@/components/info-page-layout"

export default function PaginationHelpPage() {
  return (
    <InfoPageLayout title="Pagination" subtitle="How product pages and listings are split and navigated.">
      <p>Category and collection pages are split into logical groups to keep loading fast and navigation simple.</p>
      <p>Use top navigation categories or search to jump directly to the product you need.</p>
      <p>On mobile, open the menu icon to switch between pages.</p>
    </InfoPageLayout>
  )
}
